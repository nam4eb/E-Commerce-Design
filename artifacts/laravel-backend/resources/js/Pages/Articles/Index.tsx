import { Link } from '@inertiajs/react';
import { ChevronRight } from 'lucide-react';
import SeoHead from '../../Components/SeoHead';
import StoreLayout from '../../Layouts/StoreLayout';

export default function ArticleIndex({ articles, seo, breadcrumbs }: any) {
    return <StoreLayout><SeoHead seo={seo}/><main className="container-store py-8"><nav aria-label="Breadcrumb" className="mb-5 flex gap-2 text-xs text-[#8293a8]">{breadcrumbs.map((item: any, index: number) => <span key={item.url} className="flex items-center gap-2">{index > 0 && <ChevronRight size={14}/>}<Link href={item.url}>{item.name}</Link></span>)}</nav><header className="border-b border-[#dce5ef] pb-7"><div className="text-xs font-bold uppercase tracking-[.18em] text-[#c37c0c]">Kiến thức điện máy</div><h1 className="mt-1 font-display text-4xl font-bold text-[#14385f]">Tin tức và tư vấn</h1><p className="mt-3 text-[#71869d]">Hướng dẫn chọn mua, sử dụng và bảo quản thiết bị điện máy.</p></header><section className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{articles.data.map((article: any) => <article key={article.id} className="border border-[#dce5ef] bg-white"><Link href={article.url}>{article.featured_image && <img src={article.featured_image} alt={article.title} loading="lazy" width="640" height="360" className="aspect-video w-full object-cover"/>}<div className="p-5"><time className="text-xs text-[#8293a8]">{new Date(article.published_at).toLocaleDateString('vi-VN')}</time><h2 className="mt-2 font-display text-xl font-bold text-[#14385f]">{article.title}</h2><p className="mt-2 line-clamp-3 text-sm leading-6 text-[#71869d]">{article.excerpt}</p></div></Link></article>)}</section><nav className="mt-8 flex justify-center gap-2">{articles.links.map((link: any, index: number) => link.url ? <Link key={index} href={link.url} className={`border px-3 py-2 text-sm ${link.active ? 'bg-[#0b4fa4] text-white' : 'text-[#315575]'}`}><span dangerouslySetInnerHTML={{__html: link.label}}/></Link> : null)}</nav></main></StoreLayout>;
}
